import { hankenGrotesk } from "./_hanken";
import { instrumentSans } from "./_instrument";
import { syne } from "./_syne";

export const fontNames = `${instrumentSans.variable} ${hankenGrotesk.variable} ${syne.variable}`;
